package main

import (
    "fmt"
    "sort"
)

func main() {
    numbers := []int{5, 2, 9, 1, 7}
    
    <｜fim▁hole｜>
    sort.Ints(numbers)
    
    for _, num := range numbers {
        fmt.Println(num)
    }
}